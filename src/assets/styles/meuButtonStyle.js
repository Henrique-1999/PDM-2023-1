import {Colors} from '../colors';

const MeuButtonStyle = {
  texto: {
    fontSize: 20,
    color: Colors.white,
  },

  button: {
    width: '95%',
    alignItems: 'center',
    backgroundColor: Colors.primary,
    padding: 10,
    margin: 10,
    borderRadius: 20,
  },
};

export default MeuButtonStyle;
